import { FC } from 'react';
import Logo from '@/entities/Logo/ui/ui';

interface props {}

export const ConfirmationForm: FC<props> = ({}) => {
    return (
        <>
            <Logo />
        </>
    );
};
